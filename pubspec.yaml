name: mobile2_app
description: "Prototipo Backstage"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  device_preview_plus: ^2.6.1
  flutter:
    sdk: flutter
  provider: ^6.0.5
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
